grocery_list.Hash.new
grocery_list = { "name" => name, "items" => Array.new }

def create_shopping_list
  return grocery_list
end

def create_list
  print "What is the list name? "
  name = gets.chomp

  hash 
  return hash
end

def create_shopping_list
hash = {"items" => "deli"}
return hash
end

array = Array.new

def add_list_items
  array = ["milk" "cheese" "eggs"]
  return array
end

Iteration - working with loops using each method

numbers = [0, 1, 2, 3, 4, 5]

numbers.each do |item|
  puts "The current number in the array is #{item}"
end 

hash = Hash.new

def create_shopping_list
  hash = {"title" => "Grocery List", "items" => Array.new }
   return hash
end

grocery_list = { 'title' => 'Grocery List', 'items' => [] }
grocery_item = { 'title' => 'Bread', 'quantity' => 1 }


grocery_list["items"][0] = grocery_item


loops in treehouse with ruby!



numbers = []

number = 0

loop do
  numbers[number] = number
  number = number + 1
  if numbers.length >= 3
    break
  end
end

conditional loop

answer = "e"

loop do
get_answer
  if answer = "e"
    break
  end
end

While loop

i = 0
while i < 5
  i += 1
end

Until Loop

i = 0

until i > 5
 i += 1
end
